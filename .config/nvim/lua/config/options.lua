return {
    -- You can use the system clipboard to copy and paste text
    clipboard = "unnamedplus",
    -- Hide * markup for bold and italic
    conceallevel = 3,
    -- Confirm to save changes before exiting modified buffer
    confirm = true,
    -- Enable highlighting of the current line
    cursorline = true,
    -- Ignore case when searching and replacing
    ignorecase = true,
    -- Enable mouse mode, and allow you to use the mouse to select text, scroll, and drag windows
    mouse = "a",
    -- Print line number
    number = true,
    -- Relative line numbers
    relativenumber = true,
    -- Split the current window below itself when creating a new window.
    splitbelow = true,
    -- Split the current window to the right of itself when creating a new window.
    splitright = true,
    -- Count every 4 spaces as a tab.
    tabstop = 4,
    -- Size of an indent
    shiftwidth = 4,
    -- Display short messages in the statusline.
    shortmess = "atI",
    -- Dont show mode since we have a statusline.
    showmode = false,
    -- Command-line completion mode
    wildmode = "longest:full,full",
    -- True color support
    termguicolors = true,
}
